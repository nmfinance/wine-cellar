# Аудит состояний (P21) — регрессионная карта

Матрица: каждый экран × три оси (пусто / офлайн / переполнено).
Проверено вживую на dev-сборке 14.08.2026: пустая база (все таблицы очищены),
вино с именем 97 символов + 15 сортов, дегустация с 30 ароматами и заметкой
из 400 символов без пробелов, вино без единого поля кроме имени,
несуществующие id в URL.

## Найдено и починено

| # | Экран | Состояние | Проблема → решение |
|---|-------|-----------|--------------------|
| 1 | Просмотр дегустации (и любой свободный текст) | Переполнено | Неразрывная строка в заметке растягивала страницу до 3700px (горизонтальный скролл всего экрана) → глобальный `overflow-wrap: break-word` для `p, h1-h3, summary, li` в index.css |
| 2 | Карточка вина | Переполнено | Имя 60+ символов обрезалось `line-clamp-2` на собственной карточке вина → clamp снят, имя переносится полностью (в сетке и строках клампы остались — там это правильно) |
| 3 | Карточка вина · «Глубже о вине» | Офлайн | «изучаю вино…» пульсировал вечно, если S6 не ответил → офлайн показывает честный текст сразу, онлайн — сдаёмся через 30 с; запрос повторится при следующем открытии |
| 4 | Блок «О винодельне» | Офлайн | После ошибки S2 блок исчезал навсегда без возможности повтора → при `infoStatus: 'error'` блок виден с текстом и кнопкой «обновить справку» |
| 5 | Результат винной карты | Офлайн | Тап «узнать рейтинг» при промахе молча убирал кнопку ★ без фидбека → подписи «на Vivino не нашлось» / «рейтинг подтянется при интернете» |
| 6 | FAB | — (тема) | Не было тёмного варианта (wine-600 в обеих темах) → `dark:bg-wine-400` по конвенции приложения |
| 7 | Toast | — (safe-area) | `bottom-24` без учёта жестовой полосы → `calc(6rem + env(safe-area-inset-bottom))` |
| 8 | Опросник, винная карта, результат карты | — (safe-area) | Нижний отступ `pb-8` без safe-area — контент внизу мог попасть под жестовую полосу → `pb-[calc(2rem+env(safe-area-inset-bottom))]` |
| 9 | Главный экран · поиск | Переполнено (120+ вин) | Каждый введённый символ перечитывал базу → debounce 150 мс (`useDebounced`) |
| 10 | Все экраны | Краш | Ошибка рендера давала белый экран → глобальный ErrorBoundary: карточка «Что-то сломалось · Перезагрузить» + детали под спойлером; DEV-хук `window.__crashTest` для проверки |
| 11 | PWA | Обновления | autoUpdate применялся молча при следующем запуске — владелец не знал, свежая ли версия → prompt-контур: тост «Доступна новая версия · Обновить» (skipWaiting + reload) |
| 12 | Все экраны | — (гигиена) | `document.title` был статичным → per-screen заголовки («Barolo — Мой погреб») |
| 13 | PWA | — (гигиена) | `theme-color` был статичным винным → динамический по теме: #fafaf9 / #0c0a09 (статус-бар совпадает с фоном) |

| 14 | Карта (P21.1) | Тёмная тема (прод) | Канвас выглядел сплошным чёрным. Причина: стиль OpenFreeMap `styles/dark` на малых зумах чёрный по дизайну — фон rgb(12,12,12), вода rgb(27,27,29), парки rgb(32,32,32): разница в ~15 ед. на телефоне неразличима, границы — размытые линии 23% серого. Карта «работала», но выглядела выключенной → тёмный стиль заменён на `styles/fiord` (суша #45516E / вода #38435C — читаемый контраст) |
| 15 | Карта (P21.1) | Офлайн / битый стиль / зависание | Fallback P16 ловил только сетевые ошибки с определёнными текстами и делал setStyle — а setStyle поверх УПАВШЕГО начального стиля MapLibre молча игнорирует (проверено: стиль так и не грузится, ошибок нет) → единый путь деградации: ошибка стиля ИЛИ таймаут 10 с → карта ПЕРЕСОЗДАЁТСЯ с серым fallback-стилем (точки живут) + баннер «Не удалось загрузить карту · Повторить» (кнопка пересоздаёт карту с настоящим стилем темы); `map.on('error')` всегда пишет в console.error, при `?debug=1` текст ошибки виден на экране; DEV-хук `window.__mapStyleOverride` для проверки |

| 16 | Карта (P21.2) | Тайлы не доезжают только на телефоне | Диагностировать удалённо нельзя → экран самодиагностики /map-check (кнопка «Проверить» на баннере ошибки карты + Настройки → «Диагностика карты»): 5 тестов по звеньям — style.json, реальные URL из стиля (тайл Милана/глифы/спрайт), тайл в обход SW (`cache: 'no-store'` + случайный query), WebGL-рендерер, мини-карта до события `idle` за 15 с; у каждой строки статус/время/размер или текст ошибки, «Скопировать отчёт» собирает дамп с UA. Аудит с компьютера: все ресурсы обоих стилей живут на одном хосте tiles.openfreemap.org и отвечают 200; собранный SW перехватывает только same-origin навигацию (одна NavigationRoute, runtime-кэшей нет) — кросс-доменные запросы карты не трогает |

| 17 | Карта (P21.3) | Деградация сети к зарубежному хосту | Диагноз map-check с телефона: те же URL tiles.openfreemap.org с устройства то отвечают за 221 мс (тест 3), то виснут 60+ с (2a/2b, спрайт полз 21 с) — деградация соединения на уровне сети при живом Яндекс-шлюзе → резервный маршрут: GET /tiles/{path+} в YC-функции проксирует upstream (только GET + whitelist styles/planet/natural_earth/fonts/sprites, таймаут 15 с, Cache-Control сутки — повторные сессии из браузерного кэша); клиент: режим direct/proxy в meta, `transformRequest` MapLibre переписывает префикс на шлюз; автопереключение — первая ступень пути деградации P21.1 (тост «Карта переключена на резервный маршрут», meta запоминает), вторая — прежний серый fallback; ручной переключатель и тесты 2d (тайл через шлюз) и 6 (повтор обычного после всех) — в /map-check |

| 18 | Диагностика карты (P21.4) | «стиль загружен · тайлы НЕ загружены · ошибок нет» на здоровой сети | Молчаливый вердикт не отличал «сеть не отдаёт» от «render-цикл не дошёл до запроса» → тест 5 инструментирован: transformRequest-логгер (каждый URL+тип с таймстампом; 0 тайловых запросов ≠ сеть), таймлайн событий (styledata/dataloading/sourcedata по источникам/error/render/idle), RAF-пульс нативными кадрами до и во время теста, автоматическая вердикт-строка «запросов тайлов: N · кадров: M · источники: …»; новый тест 5b — изоляция на встроенном стиле (background + растр прямыми URL, без TileJSON/вектора). Подтверждено на замороженном RAF (скрытая панель): тайловых запросов 0 при живом style/TileJSON/спрайтах — планировщик тайлов живёт в render-цикле; с живыми кадрами запросы появляются, 5b рисуется целиком. Аудит: RAF-шим из тестов P21.1 в src/dist никогда не попадал (git -S пуст, в бандле подмен нет) |

| 19 | Карта (P21.5) | Прокси-режим: 403 шлюза + ложно-зелёный тест | Два бага одного эпизода: (1) whitelist прокси проверял `startsWith('planet/')`, а TileJSON живёт на пути `planet` БЕЗ слэша → собственный 403 обезглавливал весь векторный конвейер (0 тайловых запросов); (2) 403 уезжал клиентам с `Cache-Control: public, max-age=86400` — телефон закэшировал ошибку на сутки, и починка сервера сама по себе не помогла бы → whitelist разрешает голые имена разделов, ошибки уходят с `no-store`, клиентские прокси-URL получили `?v=2` для пробива отравленных кэшей. Тест 5 стал честным: «отрисована» = idle И все источники loaded И (при векторных слоях) ≥1 тайл со статусом «ок», иначе «idle без вектора: нарисован фон»; в логе запросов статус каждого (ок/ПОВИС/ошибка с полным URL). Плюс вотчдог тайлов на основной карте: стиль loaded, но за 12 с нет idle и источники не доехали → однократное переключение на резервный маршрут (на второй карте не взводится — без циклов) |

## Проверено — уже было в порядке

- **Пусто.** Погреб/Wishlist/История — тексты-подсказки; статистика — нули + «Продегустируй первое вино…»; карта — «Карта наполняется сама…»; винная карта — подсказка + «0 из 10» в индикаторе сомелье; структура погреба — «+ Первый стеллаж»; профиль вкуса <3 дегустаций — только объяснение. Несуществующие id (`/wine/x`, `/tasting/x`, `/winelist/x`) — «не найдено», без белых экранов.
- **Офлайн.** Скан и анализ карты — баннеры «нужен интернет» (ошибки fetch не кэшируются — повтор при сети); AI-вопросы дегустации — карточка просто не показывается; геокодинг — откладывается с флагом и добирается при старте; бэкап — тосты об ошибке; CBR — тихий фолбэк на старый курс; Vivino в форме — состояние «unavailable». Vivino-промахи кэшируются только как `not_found`, сетевые ошибки — нет.
- **Переполнено.** Сетка/строки — `line-clamp`/`truncate`; 15 сортов — чипы переносятся; 30 ароматов — flex-wrap (просмотр) и топ-8 (статистика); вино без полей — «Год —», «Цена не указана», «не пробовал»; форма и опросник не разъезжаются.

## Замер производительности (120 синтетических вин + 60 дегустаций, всего 128/65)

| Операция | До (8 вин) | После (128 вин) |
|----------|-----------|-----------------|
| listFiltered (полный список) | 1.4 мс | 2.8 мс |
| Поиск по подстроке | — | 4.5 мс |
| Комбинированный фильтр (цвет+страна+дегустации) | — | 8.9 мс |
| Лента Истории | — | 1.6 мс |
| Навигация на статистику (до полного рендера) | — | ~620 мс |
| Отклик поиска в UI | — | debounce 150 мс + запрос <5 мс |

Виртуализация не нужна: слой запросов Dexie на 128 винах — единицы миллисекунд,
рендер сетки — сотни. Синтетика удалена, сиды канонические (2/1/3, 4 дегустации).

## Логи

`console.log` в src нет ни одного; отладочные сообщения — `console.debug`
(скрыты в консоли по умолчанию), ошибки — `console.error`/`console.warn`.
