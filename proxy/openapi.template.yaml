# Шаблон спеки API Gateway: {{FUNCTION_ID}} подставляет deploy.ps1.
# Шлюз нужен, потому что прямой вызов functions.yandexcloud.net/<id>
# не принимает суффиксы пути (/ai, /vivino).
openapi: 3.0.0
info:
  title: pogreb-proxy
  version: 1.0.0
paths:
  /ai:
    x-yc-apigateway-any-method:
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: "{{FUNCTION_ID}}"
  /vivino:
    x-yc-apigateway-any-method:
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: "{{FUNCTION_ID}}"
  # P21.3: резервный маршрут тайлов карты (wildcard, greedy-параметр)
  /tiles/{path+}:
    x-yc-apigateway-any-method:
      parameters:
        - name: path
          in: path
          required: true
          schema:
            type: string
      x-yc-apigateway-integration:
        type: cloud_functions
        function_id: "{{FUNCTION_ID}}"
